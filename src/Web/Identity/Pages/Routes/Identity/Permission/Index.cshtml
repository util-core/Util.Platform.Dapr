@page
<util-page-header2 title="identity.permission" sync-title="false" auto-breadcrumb="false"></util-page-header2>
@*角色信息*@
<util-alert type="Info" class="my-md">
    <util-i18n key="identity.role.currentRoleName" param="roleName:role.name" />
    <util-i18n key="app.currentApplication" param="application:selectedApplication.name" />
</util-alert>
<util-row gutter="48">
    <util-column span="4">
        <application-select (onClick)="selectApplication($event)"></application-select>
    </util-column>
    <util-column span="20">
        <util-tabset bindon-selected-index="selectedTabIndex">
            @*授予权限*@
            <util-tab lazy="true" title="identity.permission.grantPermission">
                <resources-permission #grantPermission [role]="role" [application]="selectedApplication"></resources-permission>
            </util-tab>
            @*拒绝权限*@
            <util-tab lazy="true" title="identity.permission.denyPermission">
                <resources-permission #denyPermission [role]="role" [application]="selectedApplication" [isDeny]="true"></resources-permission>
            </util-tab>
        </util-tabset>
    </util-column>
</util-row>